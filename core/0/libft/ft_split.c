#include <stdlib.h>

char	**ft_split(char const *s, char c)
{
	unsigned int	i;
	unsigned int	j;
	char			str;

	i = 0;
	j = 0;
	while (s[i])
	{
		if (s[i] == c)

	}
	str[j] = '\0';

	if (str == 0)
	{
		free (str);
		return (NULL);
	}
}
