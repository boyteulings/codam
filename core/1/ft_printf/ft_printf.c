/* notes:
 * itoa base or putnbr base?
 * probably putnbr (no base?)
 */

int	fn_flag(char *c)
{
	int	i;

	i = 0;
	while (c[i])
		i++;
	if (c[i] == '%')
	{
		i++;
		return (c[i])
	}
	else exit ()
}

int	ft_printf(const char *, )
{
}
